---
const filters = ['All', 'Frontend', 'Backend', 'Full Stack'];

const projects = [
  {
    id: 'portfolio',
    title: 'Portfolio Site',
    description: 'Personal portfolio built with Astro, Tailwind CSS, and GSAP animations.',
    tags: ['Astro', 'Tailwind', 'GSAP'],
    category: 'Frontend',
    date: '2026',
    services: ['UI/UX Design', 'Frontend Development', 'Animation'],
    details: 'A modern, static portfolio site built from scratch using Astro for zero-JS-by-default static generation, Tailwind CSS v4 for styling, and GSAP for scroll-triggered animations and horizontal slide transitions. Features light/dark mode, a 3-stage scroll system, and responsive design.',
    images: ['/images/projects/portfolio-1.png', '/images/projects/portfolio-2.png', '/images/projects/portfolio-3.png'],
    github: 'https://github.com/devhacksdotsite/jessesalinas.dev',
    website: 'https://jesse-salinas.com',
    isPrivate: false,
  },
  {
    id: 'task-app',
    title: 'Task Management App',
    description: 'Full stack task manager with real-time updates and team collaboration.',
    tags: ['React', 'Node.js', 'MySQL'],
    category: 'Full Stack',
    date: '2025',
    services: ['Full Stack Development', 'Database Design', 'REST API'],
    details: 'A collaborative task management application with real-time updates via WebSockets, role-based access control, and a clean React frontend backed by a Node.js/Express API with MySQL persistence.',
    images: [],
    github: '',
    website: '',
    isPrivate: false,
  },
  {
    id: 'rest-api',
    title: 'REST API Service',
    description: 'RESTful API with authentication, rate limiting, and comprehensive docs.',
    tags: ['Node.js', 'Express', 'Docker'],
    category: 'Backend',
    date: '2024',
    services: ['Backend Development', 'API Design', 'DevOps'],
    details: 'A production-ready RESTful API service featuring JWT authentication, rate limiting, request validation, comprehensive Swagger documentation, and Docker containerization for consistent deployment.',
    images: [],
    github: '',
    website: '',
    isPrivate: false,
  },
  {
    id: 'artventure',
    title: 'Artventure Portal',
    description: 'Art school management platform with admin and student portals for class scheduling, enrollment, and payments.',
    tags: ['React', 'Material UI', 'Node.js', 'Express', 'MySQL', 'Docker', 'AWS'],
    category: 'Full Stack',
    date: '2024 – Present',
    services: ['Full Stack Development', 'System Architecture', 'Cloud Infrastructure', 'Payment Integration'],
    details: 'Art school management platform for Artventure OC with separate admin and student portals. Manages class scheduling, student enrollment, attendance tracking, payments via Square, and instructor management. Built with React/Material UI frontend, Node.js/Express API, MySQL 8.0 database, and deployed on AWS (EC2, RDS, S3, SES, CloudFront) with Docker and Nginx. Features role-based access control, automated email notifications, and a maintenance page failover via CloudFront.',
    images: [
      '/images/projects/artventure-1.png', 
      '/images/projects/artventure-2.png',
      '/images/projects/artventure-3.png',
      '/images/projects/artventure-4.png'
    ],
    github: '',
    website: 'https://admin.artventureoc.com/admin/',
    isPrivate: true,
  },
  {
    id: 'component-lib',
    title: 'Component Library',
    description: 'Reusable UI component library with theming and accessibility built in.',
    tags: ['React', 'Tailwind', 'Storybook'],
    category: 'Frontend',
    date: '2023',
    services: ['UI/UX Design', 'Frontend Development', 'Documentation'],
    details: 'A design system and component library built with React and Tailwind CSS, documented in Storybook. Includes accessible, themeable components with full keyboard navigation and ARIA support.',
    images: [],
    github: '',
    website: '',
    isPrivate: false,
  },
  {
    id: 'data-pipeline',
    title: 'Data Pipeline',
    description: 'Automated ETL pipeline for processing and transforming large datasets.',
    tags: ['Python', 'Docker', 'AWS'],
    category: 'Backend',
    date: '2023',
    services: ['Backend Development', 'Data Engineering', 'Cloud Infrastructure'],
    details: 'An automated ETL pipeline built with Python, orchestrated via Docker containers, and deployed on AWS. Handles data ingestion, transformation, and loading with error handling, retry logic, and monitoring.',
    images: [],
    github: '',
    website: '',
    isPrivate: false,
  },
];
---

<section id="projects" class="py-24 px-6">
  <div class="max-w-6xl mx-auto">
    <div class="overflow-hidden">
      <h2 class="section-heading text-4xl font-bold mb-12 translate-y-full">Projects</h2>
    </div>

    <!-- Filter buttons -->
    <div class="flex flex-wrap gap-3 mb-10">
      {filters.map((f) => (
        <button
          class="filter-btn px-4 py-2 text-sm rounded-full border border-border text-text-muted hover:text-accent hover:border-accent transition-colors cursor-pointer"
          data-filter={f}
        >
          {f}
        </button>
      ))}
    </div>

    <!-- Project grid -->
    <div id="project-grid" class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {projects.map((p) => (
        <button
          class="project-card group block p-6 rounded-xl border border-border bg-bg-secondary hover:border-accent/50 transition-all duration-300 text-left cursor-pointer"
          data-category={p.category}
          data-project={p.id}
          data-date={p.date}
          data-services={p.services.join('|')}
          data-details={p.details}
          data-images={p.images.join('|')}
          data-github={p.github}
          data-website={p.website}
          data-private={p.isPrivate ? 'true' : ''}
        >
          <h3 class="text-lg font-semibold mb-2 group-hover:text-accent transition-colors">{p.title}</h3>
          <p class="text-text-muted text-sm mb-4">{p.description}</p>
          <div class="flex flex-wrap gap-2">
            {p.tags.map((tag) => (
              <span class="text-xs px-2 py-1 rounded bg-border/50 text-text-muted">{tag}</span>
            ))}
          </div>
        </button>
      ))}
    </div>
  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  gsap.registerPlugin(ScrollTrigger);

  const projects = JSON.parse(document.querySelector('#project-grid')?.closest('section')?.querySelector('script[type="application/json"]')?.textContent || '[]') as any[];

  // Project data (mirrored from frontmatter for client-side access)
  const projectData: Record<string, any> = {};
  document.querySelectorAll('.project-card').forEach((card) => {
    const id = card.getAttribute('data-project')!;
    projectData[id] = {
      title: card.querySelector('h3')?.textContent || '',
      description: card.querySelector('p')?.textContent || '',
      tags: Array.from(card.querySelectorAll('.bg-border\\/50')).map(t => t.textContent || ''),
    };
  });

  const dialog = document.getElementById('project-dialog')!;
  const dialogTitle = document.getElementById('dialog-title')!;
  const dialogDate = document.getElementById('dialog-date')!;
  const dialogServices = document.getElementById('dialog-services')!;
  const dialogTags = document.getElementById('dialog-tags')!;
  const dialogDetails = document.getElementById('dialog-details')!;
  const dialogImages = document.getElementById('dialog-images')!;
  const dialogLinks = document.getElementById('dialog-links')!;
  const closeBtn = document.getElementById('project-close')!;

  function openDialog(id: string) {
    const card = document.querySelector(`[data-project="${id}"]`)!;
    const title = card.querySelector('h3')?.textContent || '';
    const tags = Array.from(card.querySelectorAll('span')).map(s => s.textContent || '');
    const date = card.getAttribute('data-date') || '';
    const services = card.getAttribute('data-services') || '';
    const details = card.getAttribute('data-details') || '';
    const images = (card.getAttribute('data-images') || '').split('|').filter(Boolean);
    const github = card.getAttribute('data-github') || '';
    const website = card.getAttribute('data-website') || '';
    const isPrivate = card.getAttribute('data-private') === 'true';

    dialogTitle.textContent = title;
    dialogDate.textContent = date;
    dialogServices.innerHTML = services.split('|').map(s => `<p>${s}</p>`).join('');
    dialogTags.innerHTML = tags.map(t => `<span class="text-xs px-2 py-1 rounded bg-border/50 text-text-muted">${t}</span>`).join('');
    dialogDetails.textContent = details;
    dialogImages.innerHTML = images.map(src => `<img src="${src}" alt="${title}" class="rounded-lg border border-border w-full" loading="lazy" />`).join('');

    let linksHtml = '';
    if (isPrivate) linksHtml += `<span class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-border/50 text-text-muted text-xs"><svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg>Private Codebase</span>`;
    if (github) linksHtml += `<a href="${github}" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-border text-text-muted hover:text-accent hover:border-accent transition-colors text-sm"><svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/></svg>GitHub</a>`;
    if (website) linksHtml += `<a href="${website}" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-border text-text-muted hover:text-accent hover:border-accent transition-colors text-sm"><svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"/></svg>Website</a>`;
    dialogLinks.innerHTML = linksHtml;

    dialog.classList.remove('pointer-events-none');
    dialog.scrollTop = 0;
    document.body.style.overflow = 'hidden';
    (window as any).__dialogOpen = true;
    gsap.to(dialog, { y: 0, duration: 0.5, ease: 'power3.out' });
  }

  function closeDialog() {
    gsap.to(dialog, {
      y: '100%',
      duration: 0.4,
      ease: 'power3.in',
      onComplete: () => {
        dialog.classList.add('pointer-events-none');
        document.body.style.overflow = '';
        (window as any).__dialogOpen = false;
      },
    });
  }

  // Card clicks
  document.querySelectorAll('.project-card').forEach((card) => {
    card.addEventListener('click', (e) => {
      e.preventDefault();
      openDialog(card.getAttribute('data-project')!);
    });
  });

  closeBtn.addEventListener('click', closeDialog);

  // ESC to close
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeDialog();
  });

  // Filter buttons
  const buttons = document.querySelectorAll('.filter-btn');
  const cards = document.querySelectorAll('.project-card');

  buttons[0]?.classList.add('text-accent', 'border-accent');
  buttons[0]?.classList.remove('text-text-muted');

  buttons.forEach((btn) => {
    btn.addEventListener('click', () => {
      const filter = btn.getAttribute('data-filter')!;

      buttons.forEach((b) => {
        b.classList.remove('text-accent', 'border-accent');
        b.classList.add('text-text-muted');
      });
      btn.classList.add('text-accent', 'border-accent');
      btn.classList.remove('text-text-muted');

      const visible: Element[] = [];
      const hidden: Element[] = [];

      cards.forEach((card) => {
        const cat = card.getAttribute('data-category');
        if (filter === 'All' || cat === filter) visible.push(card);
        else hidden.push(card);
      });

      const tl = gsap.timeline();
      if (hidden.length) {
        tl.to(hidden, { opacity: 0, scale: 0.95, duration: 0.2, stagger: 0.03, onComplete: () => {
          hidden.forEach((c) => ((c as HTMLElement).style.display = 'none'));
        }});
      }
      tl.call(() => {
        visible.forEach((c) => {
          (c as HTMLElement).style.display = '';
          gsap.set(c, { opacity: 0, scale: 0.95 });
        });
      });
      tl.to(visible, { opacity: 1, scale: 1, duration: 0.3, stagger: 0.05 });
    });
  });

  // Expose animation function for the stage system to call
  // Set initial hidden state
  gsap.set('#projects .section-heading', { y: '100%' });
  gsap.set('.project-card', { opacity: 0, y: 30 });

  (window as any).animateProjects = () => {
    gsap.to('#projects .section-heading', {
      y: 0,
      duration: 0.8,
      ease: 'power3.out',
    });

    gsap.fromTo('.project-card',
      { opacity: 0, y: 30 },
      {
        opacity: 1,
        y: 0,
        stagger: 0.1,
        duration: 0.5,
        delay: 0.2,
      },
    );
  };

  // Reset function for when leaving projects
  (window as any).resetProjects = () => {
    gsap.set('#projects .section-heading', { y: '100%' });
    gsap.set('.project-card', { opacity: 0, y: 30 });
  };
</script>
